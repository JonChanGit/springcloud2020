server:
  port: 80

spring:
  application:
    name: cloud-order-service-nacos-config
  cloud:
    nacos:
      discovecloud-order-service-nacos-config-dev.ymlry:
        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式配置
        #group: DEFAULT_GROUP # 指定分组
        #namespace: e90dc63c-8138-4075-a869-bce9b2b38dd9 #填写命名空间ID
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719


#${prefix}-${spring.profile.active}.${file-extension}
# ${spring.application.name}-${spring.profile.active}.${file-extension}
# cloud-order-service-nacos-config-dev.yaml
